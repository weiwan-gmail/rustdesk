// Entry point of the v2 web protocol stack. The Flutter app is the UI; this
// module only installs the window-level bridge (setByName/getByName/init/
// isMobile) that lib/web/bridge.dart talks to.
import "./globals";
